import { supabase } from "./supabase";
export type CustomerAccess={ok:boolean;registered:boolean;verified:boolean;reason:string;checked_at?:string};
const reasonMessage:Record<string,string>={unauthorized:"Zaloguj się do konta Sunrise, aby kontynuować.",not_registered:"Najpierw załóż konto Sunrise.",missing_email:"Uzupełnij adres e-mail na koncie Sunrise.",email_not_confirmed:"Potwierdź adres e-mail konta Sunrise.",missing_profile:"Dokończ konfigurację swojego konta Sunrise.",missing_first_name:"Uzupełnij imię na koncie Sunrise.",missing_last_name:"Uzupełnij nazwisko na koncie Sunrise.",missing_phone:"Uzupełnij numer telefonu na koncie Sunrise.",email_required:"Uzupełnij prawidłowy adres e-mail konta Sunrise.",regulations_not_accepted:"Zaakceptuj regulamin konta Sunrise."};
export function customerAccessMessage(reason?:string){return reasonMessage[String(reason??"")]??"Dokończ weryfikację konta Sunrise, aby kupować i rezerwować."}
export async function refreshCustomerAccess():Promise<CustomerAccess>{const{data,error}=await supabase.functions.invoke("customer-access",{body:{}});if(error){const reason=String((data as any)?.reason??"verification_unavailable");throw new Error(customerAccessMessage(reason))}const result=data as CustomerAccess;if(!result?.ok||!result.registered||!result.verified)throw new Error(customerAccessMessage(result?.reason));return result}
